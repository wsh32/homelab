---
# Install Docker on all service VMs (everything except dev machines).
# Run after base.yml.
#
# Usage: ansible-playbook ansible/services.yml

- name: Install Docker on service VMs
  hosts: services
  gather_facts: true

  roles:
    - role: docker
