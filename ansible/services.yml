---
# Install Docker on all service VMs.
# Run after base.yml.
#
# Usage:
#   ansible-playbook ansible/services.yml

- name: Install Docker on all service VMs
  hosts: services
  gather_facts: true

  roles:
    - role: docker
