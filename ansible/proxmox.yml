---
# Apply Proxmox-specific config to physical Proxmox VE nodes.
# Usage: ansible-playbook ansible/proxmox.yml
#        ansible-playbook ansible/proxmox.yml --limit diglett

- name: Configure Proxmox nodes
  hosts: proxmox
  gather_facts: true

  roles:
    - role: proxmox
