# File-provider routes for off-host backends.
# Infra services (Infisical, Vaultwarden, Authentik) are handled by Docker labels.
# All services are LAN (.home) only until Tailscale is wired up -- see TODOS.md.
http:
  middlewares:
    # Redirect Jellyfin web UI to Authentik SSO via jellyfin-plugin-sso.
    jellyfin-sso:
      redirectRegex:
        regex: "^https://jellyfin\\.home(/web.*|/?)$"
        replacement: "https://jellyfin.home/SSO/OID/start/authentik"
        permanent: false

    # Authentik forward auth -- all arr/media services require authentication.
    # Authentik proxy providers (forward_auth_single) are bootstrapped by ansible/roles/infra/tasks/main.yml.
    authentik:
      forwardAuth:
        address: "http://authentik-server:9000/outpost.goauthentik.io/auth/traefik"
        trustForwardHeader: true
        authResponseHeaders:
          - X-authentik-username
          - X-authentik-groups
          - X-authentik-email
          - X-authentik-name
          - X-authentik-uid
          - X-authentik-jwt
          - X-authentik-meta-jwks
          - X-authentik-meta-outpost
          - X-authentik-meta-provider
          - X-authentik-meta-app
          - X-authentik-meta-version

  routers:
    # ── Traefik Dashboard ─────────────────────────────────────────────────────
    traefik-home:
      rule: "Host(`traefik.home`)"
      entryPoints: [web]
      service: api@internal
    traefik-home-tls:
      rule: "Host(`traefik.home`)"
      entryPoints: [websecure]
      service: api@internal
      tls:
        certResolver: step

    # ── diglett-dns services ──────────────────────────────────────────────────
    headplane-home:
      rule: "Host(`headplane.home`)"
      entryPoints: [web]
      service: headplane-svc
    headplane-home-tls:
      rule: "Host(`headplane.home`)"
      entryPoints: [websecure]
      service: headplane-svc
      tls:
        certResolver: step
    adguard-home:
      rule: "Host(`adguard.home`)"
      entryPoints: [web]
      service: adguard-svc
    adguard-home-tls:
      rule: "Host(`adguard.home`)"
      entryPoints: [websecure]
      service: adguard-svc
      tls:
        certResolver: step

    # ── Jellyfin ──────────────────────────────────────────────────────────────
    # SSO redirect: web UI paths go to Authentik via jellyfin-plugin-sso.
    # API/app paths (not matching /web or /) pass through directly.
    # Local admin bypass: /web/index.html#!/login.html?mode=0
    jellyfin-sso-redirect:
      rule: "Host(`jellyfin.home`) && Path(`/`)"
      entryPoints: [websecure]
      middlewares: [jellyfin-sso]
      service: jellyfin-svc
      tls:
        certResolver: step
    jellyfin-home:
      rule: "Host(`jellyfin.home`)"
      entryPoints: [web]
      service: jellyfin-svc
    jellyfin-home-tls:
      rule: "Host(`jellyfin.home`)"
      entryPoints: [websecure]
      service: jellyfin-svc
      tls:
        certResolver: step

    # ── Servarr ───────────────────────────────────────────────────────────────
    prowlarr-home:
      rule: "Host(`prowlarr.home`)"
      entryPoints: [web]
      service: prowlarr-svc
      middlewares: [authentik]
    prowlarr-home-tls:
      rule: "Host(`prowlarr.home`)"
      entryPoints: [websecure]
      service: prowlarr-svc
      middlewares: [authentik]
      tls:
        certResolver: step
    radarr-home:
      rule: "Host(`radarr.home`)"
      entryPoints: [web]
      service: radarr-svc
      middlewares: [authentik]
    radarr-home-tls:
      rule: "Host(`radarr.home`)"
      entryPoints: [websecure]
      service: radarr-svc
      middlewares: [authentik]
      tls:
        certResolver: step
    sonarr-home:
      rule: "Host(`sonarr.home`)"
      entryPoints: [web]
      service: sonarr-svc
      middlewares: [authentik]
    sonarr-home-tls:
      rule: "Host(`sonarr.home`)"
      entryPoints: [websecure]
      service: sonarr-svc
      middlewares: [authentik]
      tls:
        certResolver: step
    sabnzbd-home:
      rule: "Host(`sabnzbd.home`)"
      entryPoints: [web]
      service: sabnzbd-svc
      middlewares: [authentik]
    sabnzbd-home-tls:
      rule: "Host(`sabnzbd.home`)"
      entryPoints: [websecure]
      service: sabnzbd-svc
      middlewares: [authentik]
      tls:
        certResolver: step
    bazarr-home:
      rule: "Host(`bazarr.home`)"
      entryPoints: [web]
      service: bazarr-svc
      middlewares: [authentik]
    bazarr-home-tls:
      rule: "Host(`bazarr.home`)"
      entryPoints: [websecure]
      service: bazarr-svc
      middlewares: [authentik]
      tls:
        certResolver: step
    qbittorrent-home:
      rule: "Host(`qbittorrent.home`)"
      entryPoints: [web]
      service: qbittorrent-svc
      middlewares: [authentik]
    qbittorrent-home-tls:
      rule: "Host(`qbittorrent.home`)"
      entryPoints: [websecure]
      service: qbittorrent-svc
      middlewares: [authentik]
      tls:
        certResolver: step
    seerr-home:
      rule: "Host(`seerr.home`)"
      entryPoints: [web]
      service: seerr-svc
    seerr-home-tls:
      rule: "Host(`seerr.home`)"
      entryPoints: [websecure]
      service: seerr-svc
      tls:
        certResolver: step

    # ── PhotoPrism ────────────────────────────────────────────────────────────
    photos-home:
      rule: "Host(`photos.home`)"
      entryPoints: [web]
      service: photos-svc
    photos-home-tls:
      rule: "Host(`photos.home`)"
      entryPoints: [websecure]
      service: photos-svc
      tls:
        certResolver: step

    # ── Calibre-Web ───────────────────────────────────────────────────────────
    books-home:
      rule: "Host(`books.home`)"
      entryPoints: [web]
      service: books-svc
    books-home-tls:
      rule: "Host(`books.home`)"
      entryPoints: [websecure]
      service: books-svc
      tls:
        certResolver: step

    # ── n8n ───────────────────────────────────────────────────────────────────
    n8n-home:
      rule: "Host(`n8n.home`)"
      entryPoints: [web]
      service: n8n-svc
    n8n-home-tls:
      rule: "Host(`n8n.home`)"
      entryPoints: [websecure]
      service: n8n-svc
      tls:
        certResolver: step

    # ── CouchDB ───────────────────────────────────────────────────────────────
    couchdb-home:
      rule: "Host(`couchdb.home`)"
      entryPoints: [web]
      service: couchdb-svc
    couchdb-home-tls:
      rule: "Host(`couchdb.home`)"
      entryPoints: [websecure]
      service: couchdb-svc
      tls:
        certResolver: step

    # ── Quartz (notes) ────────────────────────────────────────────────────────
    notes-home:
      rule: "Host(`notes.home`)"
      entryPoints: [web]
      service: notes-svc
    notes-home-tls:
      rule: "Host(`notes.home`)"
      entryPoints: [websecure]
      service: notes-svc
      tls:
        certResolver: step

    # ── Homepage ──────────────────────────────────────────────────────────────
    home-home:
      rule: "Host(`home.home`)"
      entryPoints: [web]
      service: home-svc
    home-home-tls:
      rule: "Host(`home.home`)"
      entryPoints: [websecure]
      service: home-svc
      tls:
        certResolver: step

    # ── Prometheus ────────────────────────────────────────────────────────────
    prometheus-home:
      rule: "Host(`prometheus.home`)"
      entryPoints: [web]
      service: prometheus-svc
    prometheus-home-tls:
      rule: "Host(`prometheus.home`)"
      entryPoints: [websecure]
      service: prometheus-svc
      tls:
        certResolver: step

    # ── Grafana ───────────────────────────────────────────────────────────────
    grafana-home:
      rule: "Host(`grafana.home`)"
      entryPoints: [web]
      service: grafana-svc
    grafana-home-tls:
      rule: "Host(`grafana.home`)"
      entryPoints: [websecure]
      service: grafana-svc
      tls:
        certResolver: step

    # ── Proxmox ───────────────────────────────────────────────────────────────
    proxmox-home:
      rule: "Host(`proxmox.home`)"
      entryPoints: [web]
      service: proxmox-svc
    proxmox-home-tls:
      rule: "Host(`proxmox.home`)"
      entryPoints: [websecure]
      service: proxmox-svc
      tls:
        certResolver: step

  services:
    headplane-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.2:3001"
    adguard-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.2:3000"
    jellyfin-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:8096"
    prowlarr-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:9696"
    radarr-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:7878"
    sonarr-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:8989"
    sabnzbd-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:8085"
    bazarr-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:6767"
    qbittorrent-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:8080"
    seerr-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:5055"
    photos-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:2342"
    books-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:8083"
    n8n-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:5678"
    couchdb-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:5984"
    notes-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:8080"
    home-svc:
      loadBalancer:
        servers:
          # homepage maps container port 3000 to host port 3001 (grafana owns 3000)
          - url: "http://192.168.0.30:3001"
    prometheus-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:9090"
    grafana-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.0.30:3000"
    proxmox-svc:
      loadBalancer:
        # Proxmox uses a self-signed cert; serversTransport skips verification.
        serversTransport: insecureTransport
        servers:
          - url: "https://192.168.0.PROXMOX_IP:8006"

  serversTransports:
    insecureTransport:
      insecureSkipVerify: true
