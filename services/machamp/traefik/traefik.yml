api:
  dashboard: true
  insecure: false

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false
    network: bridge
  file:
    directory: /etc/traefik/dynamic
    watch: true

certificatesResolvers:
  step:
    acme:
      storage: /acme/acme.json
      caServer: https://step-ca.wsh/acme/acme/directory
      httpChallenge:
        entryPoint: web

log:
  level: INFO

accessLog: {}
