# Fetched at VM boot via: infisical export --format dotenv > /etc/homelab.env
# All values stored in Infisical (machine secrets); never committed here.

# Fetched at VM boot via: infisical export --format dotenv > /etc/homelab.env
# All values stored in Infisical (machine secrets); never committed here.

GRAFANA_ADMIN_PASSWORD=
JELLYSTAT_DB_PASSWORD=    # auto-generated by ansible/monitor.yml if absent
JELLYSTAT_JWT_SECRET=     # auto-generated by ansible/monitor.yml if absent
