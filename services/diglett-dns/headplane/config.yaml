headscale:
  url: ""  # filled in by Ansible from /etc/headscale.env
  config_path: /etc/headscale/config.yaml
  api_key: ""  # filled in by Ansible during bootstrap
  config_strict: false

server:
  base_url: ""  # filled in by Ansible
  cookie_secret_path: /var/lib/headplane/cookie.secret
  cookie_secure: false
  data_path: /var/lib/headplane

oidc:
  enabled: false              # set to true by oidc.yml after credentials are injected
  issuer: ""                  # filled in by oidc.yml
  client_id: ""               # filled in by oidc.yml
  client_secret: ""           # filled in by oidc.yml
  headscale_api_key: ""       # filled in by oidc.yml
  disable_api_key_login: false
